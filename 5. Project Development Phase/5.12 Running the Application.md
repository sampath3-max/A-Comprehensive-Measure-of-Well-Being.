# Running the Application

## Objective

This step explains how to run the HDI Prediction System locally.

## Step 1: Install Dependencies

Install all required Python packages using:

```bash
pip install -r requirements.txt
