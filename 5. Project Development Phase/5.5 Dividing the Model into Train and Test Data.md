# Dividing the Model into Train and Test Data

## Objective

The dataset is divided into training and testing datasets so that the Machine Learning model can be trained and evaluated.

## Train-Test Split

The project uses the `train_test_split` function from Scikit-learn.

```python
Xtr, Xte, ytr, yte = train_test_split(
    X,
    y,
    test_size=.2,
    random_state=42
)
