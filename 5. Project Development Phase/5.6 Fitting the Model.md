# Fitting the Model

## Objective

The objective of this step is to train a Machine Learning model using the training data.

## Machine Learning Algorithm

The project uses the Linear Regression algorithm.

Linear Regression is used because the system predicts a continuous numerical value, which is the HDI Score.

## Creating the Model

The Linear Regression model is created using Scikit-learn.

```python
m = LinearRegression()
