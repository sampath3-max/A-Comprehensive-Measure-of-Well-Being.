# Saving the Model

## Objective

After training the Linear Regression model, the trained model is saved so that it can be reused by the Flask web application.

## Model Serialization

The Python `pickle` module is used to save the trained model.

```python
pickle.dump(m, open('HDI.pkl', 'wb'))
