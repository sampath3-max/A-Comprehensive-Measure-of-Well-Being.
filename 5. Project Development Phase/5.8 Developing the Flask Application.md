# Developing the Flask Application

## Objective

The Flask application provides the web interface for the HDI Prediction System.

It connects the user interface with the trained Machine Learning model.

## Creating the Flask Application

The Flask application is created using:

```python
app = Flask(__name__)
